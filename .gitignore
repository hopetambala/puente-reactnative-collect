node_modules/
coverage/
expo-resources/
.expo
.vscode
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
logs/

# macOS
.DS_Store*
.env*
scripts/dynamic-env/app.secrets.json
scripts/update-version/app.secrets.json
app.generated.json

# Virtual Env
#
reactenv/
scripts/update-version/app.secrets.json

# Stuff for release builds
# (e.g. for EAS)
environment.js
app.json

# Regenerated by `expo prebuild`; EAS excludes android/ via .easignore and
# prebuilds from app.json, so nothing in here is authoritative.
android/
# ios/

# ─── Local Claude Code setup — deliberately NOT versioned ───────────────────
# This repo is public; the Claude configuration is personal tooling, not part
# of the app. NOTE: .claude/ was tracked before 2026-08-28 (first added in
# 3a8b0dd), so it still exists in git history; this only stops future churn.
.claude/

