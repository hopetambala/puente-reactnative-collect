import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  header: {
    fontWeight: "bold",
    marginTop: 10,
  },
  container: {
    flex: 1,
  },
});

export default styles;
